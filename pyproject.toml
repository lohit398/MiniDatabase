[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "minidatabase"
version = "0.1.0"
description = "A mini relational database with an LLM-assisted query planner"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
