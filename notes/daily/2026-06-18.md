# 2026-06-18 — REST DAY (taken)

_Rest day for the week — insufficient sleep the night before. Health non-negotiable, used correctly. The week-1 close-push plan below did not run today; it carries forward to the next working day unchanged. No guilt: rest is what makes the next block sharp._

---

# Week-1 close push (Parser Day 7) — carries to next working day

## Where you left off (Tuesday June 17)

- AND/OR combinators shipped, **precedence correct** (OR root, AND deeper), case-leak closed, 16 green, **committed**.
- Lesson burned in: green lied 3x, the by-hand probe caught all 3. Tests must be derived from the *spec* and go **red first**.
- Contract (`project.md`) tightened: read-it-first banner, definition-of-done discipline, week-close discipline, anti-patterns.
- Hard truth on the table: **week 1 is ~2.5–3x over budget and still below its done-bar.** Today is a *close-the-week* day, not an add-features day.

> The mindset for today: stop adding parser cleverness. The week-1 done-bar is a **runnable command** — make that command exist and pass. Don't start week 2 anything.

---

## Week-1 done-bar — the actual finish line (from the contract)

> Done when: `minidb parse "SELECT name, age FROM users WHERE age > 30 AND country = 'US'"` prints a clean AST. Tests pass. 30+ parse tests, 90%+ coverage.

Open clauses (this is the checklist to close, in priority order below):
- [ ] CLI command that prints the AST  ← the headline; nothing else matters until this runs
- [ ] module-level `parse(query)` (PARSER-3)
- [ ] package/CLI name resolved (`tinydb` vs `minidb`)
- [ ] 30+ parse tests (have ~16)
- [ ] 90%+ coverage (not measured; `pytest-cov` not installed)
- [ ] table aliases (`users u`) — `Table.alias` currently hardcoded None
- [ ] source-position error messages polished

---

## Today's queue, in order (don't reorder — close top-down)

### 1. Decide the name: `tinydb` vs `minidb` (10 min, FIRST — it's a blocker)

The contract's done-bar command is `minidb parse ...`; the package is `tinydb`. The command can't be typed as written until they agree. **One decision.** Pick one, and if you rename, do it cleanly (package dir, imports, `pyproject.toml`, egg-info). Don't half-rename. This is option-shopping bait — decide in 10 min and move.

### 2. Module-level `parse(query: str)` — PARSER-3 (30 min)

A single function that takes raw SQL text, runs the tokenizer, runs the parser, returns the AST. Right now callers must tokenize-then-`Parser(tokens).parse()` by hand. The CLI needs one clean entry. **Done-bar:** `from tinydb import parse; parse("SELECT * FROM users")` returns the AST. Probe by hand before trusting.

### 3. CLI entrypoint — the done-bar command (45 min)

`<name> parse "<sql>"` prints the AST to stdout. Wire it as a `[project.scripts]` console entry in `pyproject.toml` (or a `__main__`). **Done-bar (literal, from the contract):**
```
<name> parse "SELECT name, age FROM users WHERE age > 30 AND country = 'US'"
```
prints a clean AST with AND at the root, two comparison children. Run that exact command and eyeball the tree. That's the week-1 headline met.

### 4. Tests to 30+ and coverage measured (45 min)

- `pip install pytest-cov` into the venv; run `.venv/bin/python -m pytest --cov=<pkg> --cov-report=term-missing`.
- You have ~16. Get to **30+** by covering the gaps the coverage report shows — error paths especially (trailing comma, missing FROM, bad operator, unterminated input, non-SELECT first token). Write each test **from the spec, red-first**, then make it pass. No tests shaped to match current output.
- **Done-bar:** 30+ tests green, coverage ≥90% on the parser/tokenizer.

### 5. If time remains — table aliases (contract day-5 clause)

`SELECT u.name FROM users u` — populate `Table.alias` instead of hardcoded None. Only start this if 1–4 are done; otherwise log it and stop. **Not** today: JOIN parsing, arithmetic, INSERT/UPDATE.

---

## Deferred-debt cleanup (fold into the work, ~10 min total)

- **PARSER-9** — delete unused `isEnd()` / `previous()`. You deferred this to today's reference pass; do it.
- **Tokenizer:** lone `!` is silently dropped (no token, no error). Add an error path or log it as TOKENIZER-7. Don't leave input silently swallowed.

---

## CP — reassessment

The 5-day calibration reassessment is due (Thursday). Before you decide the band:
- Fill the blank `Time` and `Needed editorial?` columns in `notes/cp/2026-06.md` for yesterday's 459B / 476B — and **confirm their real ratings** on the pages (I estimated ~1500 / ~1400).
- Tally the week's **clean**-solve rate (editorial-needed ≠ clean). Apply the rule: <50% drop / 50–70% stay / >70% climb. Yesterday's two were above the 1200–1300 band — if clean, that's evidence to climb.
- **Today's problem: CF 1931D — https://codeforces.com/problemset/problem/1931/D** (confirm rating on page). 60-min hard box, classify cold before any hint, log result (rating / solved / time / editorial-needed) in `notes/cp/2026-06.md`.

---

## Total session estimate

Name (10) + module parse (30) + CLI (45) + tests/coverage (45) + debt (10) ≈ 2h20. Aliases only if it fits. Fits the 5:30 block.

## Tonight (end-of-day)

Write `notes/daily/2026-06-18.md` retrospective. **State plainly whether week 1 is closed** — every done-bar clause true or not. If closed, write the `notes/weekly/week-01.md` review (contract day 7). Commit. Sleep 7+.
