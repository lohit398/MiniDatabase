# 2026-05-31 — Sunday

## What shipped today

- **CP 2203/C re-implementation from memory** ✓ — full deliberate-practice loop closed (attempted → editorial studied → re-implemented next day). The beautiful bit-manipulation + binary-search pattern from yesterday's editorial.
- **Crafting Interpreters Ch 5 (Representing Code)** ✓ — read end to end. Covered: BNF notation, terminal vs non-terminal, derivation as repeated substitution, EBNF quantifiers (`?` zero-or-one, `*` zero-or-more, `+` one-or-more), grouping with `( ... )`, alternation with `|`. Breakfast grammar mechanics internalized.
- **45-min swim** ✓ — cardio variety. Continues this week's exercise non-negotiable.
- **Conceptual conversations** with coach: lexical vs context-free grammars and why each is needed (regular grammars can't count nesting; CFGs need a stack); Mad Libs analogy for derivation; full walk-through of why parens/`|` are EBNF shorthand for what would otherwise be multiple-rule alternatives.

## What slipped

- **Parser day 3 deliverable** — `parse("SELECT * FROM users")` → SelectStmt AST. Did not start. Carries to tomorrow.

## Tomorrow

In order:
1. **CP** — new problem, daily protocol (phone in another room, frame as practice not solve, algorithm-class enumeration when stuck at ~15 min).
2. **CI Ch 6 (Parsing Expressions)** — reading block.
3. **Parser day 3** — AST node dataclasses (`SelectStmt`, `Star`, `Table` carrying line/col), Parser class skeleton (`__init__`, `advance/peek/match/expect` helpers), `parseSelectStmt()` for the star case only, one test (`test_parse_select_star`). Green by end of session.

Note: parser day 3 only needs Ch 5's AST framing — can be shipped *before* reading Ch 6. Ch 6 is what unlocks day 4 onward.

## Outstanding

- **4:30 AM schedule migration** — target was ~June 2 (Tuesday, day after tomorrow). Status TBD.
- **Tokenizer backlog** — 6 items in `notes/backlog.md`, resolve before parser error reporting (estimated Week 1 Day 5–6).
