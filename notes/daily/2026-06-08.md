# 2026-06-08 — Sunday

## Where you left off (Saturday June 7)

- **Parser day 3 SHIPPED.** 6/6 tests green. Star case `parse("SELECT * FROM users")` works end-to-end with proper ParseError, helpers, dataclass construction at end. The 6-day slip is over.
- **CP calibration trial STARTED.** Solved one 1200-rated problem. Started Codeforces 2230/C (status from last night not confirmed — check first thing).
- **Backlog grew:** PARSER-3 through PARSER-7 added to `notes/backlog.md`. None block today.

---

## Today's queue, in order

### 1. Parser day 4 — column lists (FIRST)

**Deliverable:** `parse("SELECT a, b, c FROM users")` returns a SelectStmt AST with three Column nodes in `columns`, each carrying its IDENTIFIER token. Also `parse("SELECT a FROM users")` works (single named column). The existing star case (`SELECT * FROM users`) must STILL pass.

**Time budget:** 1.5–2 hours.

**What changes in parseSelectStmt():** the part where you currently do `consume(STAR)` and append a single Column becomes a sub-parser for the column list. Pattern (from the EBNF → primitive table):

- `Column → "*" | IDENTIFIER` (single column = CHOICE)
- `ColumnList → Column ( "," Column )*` (one column, then zero-or-more comma-prefixed extras = REPETITION)

In code shape (not the literal code — you write it):
- **First column:** peek the next token. If STAR → consume STAR, build Column with the star token. Elif IDENTIFIER → consume IDENTIFIER, build Column with that token. Else → ParseError ("expected `*` or column name").
- **Subsequent columns:** `while match(COMMA): parse another column (same choice as above), append to columns list`.
- Then `consume(KW_FROM)` continues as before.

**New AST nodes you may want:** either keep one `Column` class with `token: Token` (current shape — discriminator via `token.type`), OR introduce separate `Star` and `Identifier` classes for cleaner pattern-matching. Your call — both work. If you go separate-classes route, update the test assertion (`token.type` → `isinstance(col, Star)`).

**Tests to add:**
- `test_parser_single_column` → `SELECT a FROM users` → columns = [Column(IDENTIFIER "a")], no extras
- `test_parser_multi_column` → `SELECT a, b, c FROM users` → three Columns in order
- `test_parser_star_still_works` → original star case (ensure no regression)
- `test_parser_trailing_comma_errors` → `SELECT a, FROM users` → expects ParseError (good edge case)

Stop when all four green. Don't add more features today.

---

### 2. CP — second day of the 1200-1300 trial

**Deliverable:** one new 1200-1300 problem, **60-min HARD box**, phone in another room, frame as practice not solve, algorithm-class enumeration at ~15 min stuck.

Also: if 2230/C from last night is unfinished, the call is — editorial it. Don't re-grind. Re-implementation tomorrow if it was a real loss.

**Tracking:** add today's attempt to `notes/cp/` — date, problem rating, solved (Y/N), time taken. Build the dataset for the 5-day calibration call (Thursday).

**Time budget:** 60 min CP + 15 min editorial/notes if needed.

---

### 3. CI Ch 6 (Parsing Expressions) — finish

**Deliverable:** read to the end of the chapter, including the Pratt/precedence section.

**Why now:** you've shipped day 3 and day 4 (after this morning's block) without Ch 6. Day 5 (WHERE clauses + expression parsing with precedence) **needs** Ch 6 — that's where Pratt parsing is taught. Reading it tonight prevents day 5 from stalling.

**Time budget:** 30–45 min.

---

## Total session estimate

Parser day 4 (90 min) + CP (60 min) + Ch 6 (30 min) ≈ 3 hours. Fits your 5:30 AM block.

## Backlog reminder (don't touch today)

PARSER-3 through PARSER-7 in `notes/backlog.md` — none block day 4. Resolve when their trigger conditions arrive.

## Tonight (end-of-day)

Log what shipped, what slipped. Honest count of CP problems solved this week for the calibration trial. Sleep 7+ for the 5:30 AM wake.
