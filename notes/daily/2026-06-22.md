# 2026-06-22 — Back from fever; Week 1 CLOSED

_Recovered from a 4-day fever (2026-06-18 → 21). Health non-negotiable, used correctly — no guilt. First working day back. Goal today: close week 1 to its actual done-bar, nothing new._

---

## What got done

- **Tests 16 → 32**, all real (no padding). Added attacking tests: error paths (no columns, `*,id`, `*,*`), unterminated string, `4.56.1` double-dot, `|` vs `||`, lone `!`, tree-shape assertions on AND/OR precedence.
- **Fixed two theater tests:**
  - `test_peek_fun_out_of_bounds` — was asserting on the Tokenizer while claiming to test the Parser, with a string-vs-enum comparison that could never pass. Now calls `p.peek/advance/match/consume` → `None`.
  - `test_pos` — set `t.pos` (a phantom attribute the tokenizer never reads); assertions passed incidentally. Changed to `t.index` so the setup is live and actually exercises the out-of-bounds guard.
- **Coverage ≥90% branch** on the files that matter: Parser 93%, Tokenizer 93%, ast 100%. (`__init__.py` 30% — the CLI/`main()` glue; `parse()` itself is exercised everywhere.)
- **Table aliases** — implemented both `FROM users u` (the contract's implicit form, day-5 clause) and `FROM users AS x`. Negative case (`FROM users` → `None`) and with-WHERE both verified.

## Where it lied (the 4th green-lie this week)

- First alias attempt only handled `AS usrs` — the contract's clause is the *implicit* `users u`, which returned `None`. Both alias tests asserted the `AS` form, so the suite went green while the headline form was broken. Same anti-pattern: **tests shaped to confirm what was built, blind to the spec.** The by-hand probe caught it again. Green has now lied 4× on this project (parseOperand, AND combinator, alias form, and the trailing-token swallow below). The probe is the discipline — run it *before* saying done, every time.

## Two pre-existing bugs surfaced (deferred to 2026-06-23, by design)

- **TOKENIZER-7** — `a>5` (no space) drops the `5` silently → `[IDENTIFIER, GT, EOF]`. Pre-existing; every WHERE test spaces its operators, so never caught.
- **PARSER-11** — `parse()` never asserts EOF, so trailing/malformed tokens are silently swallowed (`FROM users blah blah WHERE a>5` drops the WHERE). Pre-existing; aliases just made the symptom reachable.
- **Decision (mine, correct):** neither is the same day's planned work, neither is a week-1 done-bar clause. They go to backlog on schedule (tomorrow), they do **not** halt the week. Coach over-stepped by initially framing PARSER-11 as a week-1 blocker — corrected. Found-today ≠ caused-today; using a latent bug I didn't catch at original ship to block a met milestone moves the goalposts.

## Week 1 — CLOSED (verified clause-by-clause)

| Clause | Verified |
|---|---|
| `minidatabase parse "SELECT name, age FROM users WHERE age > 30 AND country = 'US'"` → clean AST | ✅ AND at root, two comparison children |
| Tests pass | ✅ 32 green |
| 30+ parse tests | ✅ 32, all real |
| 90%+ coverage | ✅ branch, parser + tokenizer |
| Table aliases (`users u`) | ✅ implicit + AS + negative |
| Name resolved | ✅ `minidatabase` |

Honest note: week 1 ran far over its 7-day budget. The deliverable is real and shippable; the time cost is the lesson, logged plainly — no day-renumbering.

## CP

- Solved **CF 1285B Just Eat It!** — rating **1300** (page-confirmed; coach wrongly guessed ~1700 from memory, corrected), **clean, 60 min**. Logged in `notes/cp/2026-06.md`.
- Read: a clean solve at the *top* of the band taking the *full* time box is a "stay" signal, not "climb."
- Reassessment **still blocked**: 459B/476B `Time`/`editorial` columns remain unscored — they're the only above-band evidence. Decision waits on filling them.

## Process change carried forward

- **Unrelated bugs wait.** A bug discovered mid-session that isn't part of today's planned, related work gets backlogged — it does not get fixed now and does not derail the current focus. It waits until today's work is done, or until the day it's scheduled. (Same principle that keeps a latent bug from blocking a met milestone.)

## Tomorrow's first action

Open `notes/daily/2026-06-23.md` (already drafted). First: TOKENIZER-7, repro-then-red-test-then-fix. Then PARSER-11 (EOF guard). Only after both green + committed: week-2 `Operator` interface + `SeqScan`.

## Still owed tonight

- `notes/weekly/week-01.md` — the week-1 review (contract day 7). Write while the 4 green-lies are fresh; they are the real lesson of the week.
- Commit everything. Sleep 7+.
